apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-rc3-clean
  namespace: default
  labels:
    app: nginx-rc3-clean
    test: v0-2-9-rc-3-anthropic-provider
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-rc3-clean
  template:
    metadata:
      labels:
        app: nginx-rc3-clean
    spec:
      volumes:
        - name: host-etc
          hostPath:
            path: /etc
      containers:
        - name: nginx
          image: nginx:latest
          ports:
            - containerPort: 80
          securityContext:
            privileged: true
            allowPrivilegeEscalation: true
            runAsUser: 0
            capabilities:
              add:
                - SYS_ADMIN
                - NET_RAW
          volumeMounts:
            - name: host-etc
              mountPath: /host-etc
